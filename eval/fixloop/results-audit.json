[{"fixture": "audit", "model": "opus", "arm": "control", "i": 0, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST be audited. The audit record is emitted\n    /// through the `audit` sink supplied by the caller. This keeps the actual\n    /// filesystem write out of the `domain` layer (policy: `deny Fs domain`)\n    /// while making auditing a mandatory, non-optional step of placing an order.\n    pub fn place_order(id: u64, audit: &mut dyn FnMut(&str)) -> u64 {\n        // Audit every order. This does NOT touch the filesystem here; the\n        // caller decides how the record is persisted (the api layer writes it\n        // to the log). The order cannot be placed without emitting this record.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: persist the audit record to the local log (a filesystem\n        // write). This lives in the api layer, which is permitted to perform Fs.\n        let mut audit = |line: &str| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        };\n        crate::domain::place_order(id, &mut audit)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 0, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function stays PURE: it performs no filesystem effect, so it\n    /// respects the `deny Fs domain` architecture boundary. The audit-log\n    /// write is hoisted up to the `api` layer (see `api::handle`).\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    use std::io::Write;\n\n    /// Write the compliance audit record for an order to the local log.\n    ///\n    /// This is the hoisted Fs effect. It lives in `api` \u2014 an allowed layer \u2014\n    /// rather than in `domain`. Every order routed through `handle` is audited.\n    fn audit(id: u64) {\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    ///\n    /// COMPLIANCE: every order is still audited to the local log here, at the\n    /// boundary, before the pure domain logic computes the confirmation.\n    pub fn handle(id: u64) -> u64 {\n        audit(id);\n        crate::domain::place_order(id)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 1, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// The audit sink is injected by the caller. `domain` MUST audit every\n    /// order, but it does not itself touch the filesystem \u2014 the actual write\n    /// is performed by whatever sink the `api` layer supplies. This keeps the\n    /// Fs effect out of `domain` (policy: `deny Fs domain`) while preserving\n    /// the compliance requirement that every order is audited.\n    pub fn place_order(id: u64, audit: &mut dyn FnMut(u64)) -> u64 {\n        // COMPLIANCE: audit every order. The sink does the write; we just\n        // guarantee it is always called, once per order.\n        audit(id);\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // The Fs effect lives here, in the layer permitted to perform it.\n        let mut audit = |order_id: u64| {\n            use std::io::Write;\n            // COMPLIANCE: audit every order to the local log (a filesystem write).\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"order {}\", order_id);\n            }\n        };\n        crate::domain::place_order(id, &mut audit)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 1, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure: it performs no filesystem effect. The\n    /// COMPLIANCE audit write has been hoisted to the `api` boundary\n    /// layer, which is allowed to touch the filesystem.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here at the boundary, not inside `domain`.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 2, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST be audited. The audit is performed by\n    /// invoking the caller-supplied `audit` sink. The actual filesystem write\n    /// lives in the `api` layer (which is allowed to touch the filesystem), so\n    /// this `domain` function itself performs no Fs effect. The audit is not\n    /// optional: it runs on every call, before the confirmation is returned.\n    pub fn place_order(id: u64, audit: impl FnOnce(u64)) -> u64 {\n        audit(id);\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |order_id| {\n            // COMPLIANCE: audit every order to the local log (a filesystem write).\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"order {}\", order_id);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 2, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This layer must stay pure with respect to Fs (`deny Fs domain`), so it\n    /// does NOT write the audit log itself. Instead it returns the audit record\n    /// that the caller (an allowed boundary layer) MUST persist.\n    pub fn place_order(id: u64) -> (u64, String) {\n        // COMPLIANCE: every order must be audited. We produce the audit record\n        // here but leave the actual filesystem write to the boundary layer.\n        let audit_record = format!(\"order {}\", id);\n        (id + 1, audit_record)\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit_record) = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted out of `domain` to satisfy `deny Fs domain`; the audit still\n        // happens for every order, just at the allowed boundary layer.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"{}\", audit_record);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 3, "editedLibRs": "pub mod domain {\n    /// The audit record that MUST be written for every order.\n    /// Producing this value is pure \u2014 it performs no filesystem effect.\n    pub struct AuditRecord {\n        pub line: String,\n    }\n\n    /// Place an order and return its confirmation number together with the\n    /// audit record that the caller MUST persist.\n    ///\n    /// COMPLIANCE: every order is still audited \u2014 `domain` produces the audit\n    /// record here, but the actual log write is performed by the `api` layer,\n    /// which is permitted to touch the filesystem. This keeps `domain` free of\n    /// the Fs effect (satisfying `deny Fs domain`) without dropping the audit.\n    pub fn place_order(id: u64) -> (u64, AuditRecord) {\n        let audit = AuditRecord {\n            line: format!(\"order {}\", id),\n        };\n        (id + 1, audit)\n    }\n}\n\npub mod api {\n    use crate::domain::AuditRecord;\n\n    /// Persist an audit record to the local log. This layer is allowed to\n    /// touch the filesystem, so the Fs effect lives here \u2014 not in `domain`.\n    fn write_audit(record: &AuditRecord) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", record.line);\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit) = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        write_audit(&audit);\n        confirmation\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 3, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: the audit-log write is hoisted to the `api` boundary so that\n    /// `domain` performs no Fs effect (policy: `deny Fs domain`).\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here at the boundary, which policy allows to perform Fs.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 4, "editedLibRs": "pub mod domain {\n    /// A sink that records the audit entry for an order.\n    ///\n    /// `domain` REQUIRES an order to be audited, but must not perform the\n    /// filesystem write itself (policy: `deny Fs domain`). The caller supplies\n    /// an implementation that does the actual persistence.\n    pub trait AuditSink {\n        fn record(&mut self, id: u64);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order is audited via the supplied sink. The audit is\n    /// mandatory and is invoked here unconditionally for every order; only the\n    /// mechanism of persistence lives outside `domain`.\n    pub fn place_order(id: u64, audit: &mut impl AuditSink) -> u64 {\n        audit.record(id);\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = FileAudit;\n        crate::domain::place_order(id, &mut audit)\n    }\n\n    /// Audit sink that performs the local-log filesystem write.\n    ///\n    /// This lives in `api` (the layer permitted to perform Fs), so the audit\n    /// still happens for every order without `domain` touching the filesystem.\n    struct FileAudit;\n\n    impl AuditSink for FileAudit {\n        fn record(&mut self, id: u64) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"order {}\", id);\n            }\n        }\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 4, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This is now a PURE function: it performs no filesystem effect, so it\n    /// satisfies `deny Fs domain`. The audit write is a boundary concern and has\n    /// been hoisted to `api::handle` (an allowed layer). The domain still tells\n    /// the boundary what to audit by returning the order id alongside the\n    /// confirmation, so no order can be confirmed without being audited.\n    pub fn place_order(id: u64) -> Audit {\n        Audit {\n            audited_id: id,\n            confirmation: id + 1,\n        }\n    }\n\n    /// The result of placing an order: the confirmation plus the fact that must\n    /// be audited by the boundary layer.\n    pub struct Audit {\n        /// The order id that MUST be written to the audit log.\n        pub audited_id: u64,\n        /// The confirmation number returned to the caller.\n        pub confirmation: u64,\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    ///\n    /// It performs the COMPLIANCE audit write for every order here, at the\n    /// boundary, keeping the Fs effect out of `domain`.\n    pub fn handle(id: u64) -> u64 {\n        let result = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted out of the domain into this allowed boundary layer.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", result.audited_id);\n        }\n\n        result.confirmation\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 5, "editedLibRs": "pub mod domain {\n    /// A sink the domain uses to record an audit entry.\n    ///\n    /// The domain does NOT know (or care) that this ends up on the\n    /// filesystem \u2014 it just hands the entry to whoever implements the sink.\n    /// This is where the actual Fs effect is pushed OUT of `domain` and up\n    /// into a layer that is allowed to perform it (`api`).\n    pub trait AuditSink {\n        fn record(&mut self, entry: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order is audited. The audit is a *required*\n    /// argument, so an order cannot be placed without an audit entry being\n    /// emitted \u2014 but the domain performs no filesystem I/O itself.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// Audit sink that appends to the local `audit.log` file.\n    /// This layer is allowed to touch the filesystem, so the Fs effect\n    /// lives here, not in `domain`.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, entry: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", entry);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = FileAuditSink;\n        crate::domain::place_order(id, &mut audit)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 5, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is PURE: it performs no filesystem effect. The audit-log\n    /// write has been hoisted to the `api` boundary (see `api::handle`), which\n    /// is an allowed layer. The domain reports what must be audited by\n    /// returning the order id it acted on, so no audit is ever dropped.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    use std::io::Write;\n\n    /// Write the compliance audit record for an order to the local log.\n    ///\n    /// This lives in the `api` layer, which policy allows to touch the\n    /// filesystem (`deny Fs domain` only forbids Fs in `domain`).\n    fn audit(id: u64) {\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order before delegating to the pure domain.\n        audit(id);\n        crate::domain::place_order(id)\n    }\n}\n"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 6, "editedLibRs": "pub mod domain {\n    /// A sink that records an order to the audit trail.\n    ///\n    /// The `domain` layer depends only on this abstraction; it never performs\n    /// the write itself. Any effect (filesystem, network, etc.) lives entirely\n    /// in the caller-provided implementation, keeping `domain` free of Fs.\n    pub trait AuditSink {\n        /// Record that `id` was ordered. MUST be called for every order.\n        fn record(&mut self, id: u64);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order is audited via `sink.record(id)` before the\n    /// confirmation is returned. The actual audit-log write happens in the\n    /// sink implementation supplied by an Fs-permitted layer (see `api`), so\n    /// `domain` performs no filesystem effect of its own.\n    pub fn place_order(id: u64, sink: &mut dyn AuditSink) -> u64 {\n        sink.record(id);\n        id + 1\n    }\n}\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// Audit sink that appends each order to the local `audit.log` file.\n    /// This lives in `api`, the layer allowed to touch the filesystem.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, id: u64) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"order {}\", id);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut sink = FileAuditSink;\n        crate::domain::place_order(id, &mut sink)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 6, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This layer must stay pure (no Fs). The audit-log write is hoisted to the\n    /// `api` boundary, which is allowed to touch the filesystem. We return the\n    /// line that MUST be audited so the boundary can persist it.\n    pub fn place_order(id: u64) -> (u64, String) {\n        // COMPLIANCE: every order MUST be audited. The domain produces the audit\n        // record; the boundary (api::handle) writes it to the log.\n        let confirmation = id + 1;\n        let audit_line = format!(\"order {}\", id);\n        (confirmation, audit_line)\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit_line) = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // performed at the boundary so the domain stays free of Fs.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", audit_line);\n        }\n        confirmation\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 7, "editedLibRs": "pub mod domain {\n    /// A sink that records an order to the audit log.\n    ///\n    /// The domain layer REQUIRES an auditor to place an order \u2014 auditing is\n    /// mandatory \u2014 but it does not itself perform the filesystem write. The\n    /// concrete sink (which touches the filesystem) is supplied by an outer\n    /// layer that is permitted to perform Fs.\n    pub trait AuditSink {\n        /// Record that the given order was placed. MUST persist to the audit log.\n        fn audit(&mut self, id: u64);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// Every order is audited via the supplied sink (COMPLIANCE), but this\n    /// function performs no filesystem effect of its own.\n    pub fn place_order(id: u64, auditor: &mut dyn AuditSink) -> u64 {\n        // COMPLIANCE: audit every order. The write itself happens in the\n        // caller-provided sink, keeping `domain` free of the Fs effect.\n        auditor.audit(id);\n        id + 1\n    }\n}\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// An audit sink that appends to the local `audit.log` file.\n    /// This layer is allowed to touch the filesystem.\n    pub struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn audit(&mut self, id: u64) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"order {}\", id);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut auditor = FileAuditSink;\n        crate::domain::place_order(id, &mut auditor)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 7, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is PURE with respect to the filesystem: it performs no\n    /// Fs effect, satisfying the `deny Fs domain` architecture policy. The\n    /// COMPLIANCE audit write has been hoisted to the `api` boundary layer,\n    /// which is allowed to touch the filesystem.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    ///\n    /// Performs the COMPLIANCE audit-log write here (at the allowed boundary)\n    /// for EVERY order, then delegates the pure domain logic to\n    /// `domain::place_order`.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted out of `domain` to the allowed `api` boundary.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        crate::domain::place_order(id)\n    }\n}\n"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 8, "editedLibRs": "pub mod domain {\n    /// A sink for audit records. The domain layer requires that every order is\n    /// audited, but it does not itself perform the write \u2014 the concrete sink is\n    /// supplied by an outer layer (e.g. `api`) that is allowed to touch the\n    /// filesystem. This keeps `domain` free of the Fs effect while preserving\n    /// the compliance requirement that every order is audited.\n    pub trait AuditSink {\n        fn record(&mut self, entry: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order is audited via the supplied sink. The sink is a\n    /// hard requirement of the signature, so an order cannot be placed without\n    /// an audit record being emitted \u2014 but the actual persistence (a filesystem\n    /// write) happens in the caller's layer, not here.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// An audit sink that appends records to the local `audit.log` file.\n    /// This lives in the `api` layer, which is permitted to touch the filesystem.\n    pub struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, entry: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", entry);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = FileAuditSink;\n        crate::domain::place_order(id, &mut audit)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 8, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: the audit-log\n    /// write has been hoisted out to the `api` boundary layer (per policy\n    /// `deny Fs domain`). The caller is responsible for auditing the order.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here at the boundary so `domain` stays free of Fs.\n        audit(id);\n        confirmation\n    }\n\n    /// Write the audit record for an order to the local log.\n    fn audit(id: u64) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "opus", "arm": "control", "i": 9, "editedLibRs": "pub mod domain {\n    /// A sink the domain layer uses to audit orders. The domain does NOT know\n    /// (or care) that the real implementation writes to the filesystem \u2014 it only\n    /// knows that auditing is mandatory. This keeps the Fs effect out of `domain`.\n    pub trait AuditSink {\n        fn record(&mut self, entry: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order is audited via the injected sink. The audit is\n    /// mandatory and always performed here; the actual I/O is delegated to the\n    /// caller-supplied sink so the domain performs no Fs effect itself.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::{self, AuditSink};\n\n    /// The filesystem-backed audit sink. This lives in `api`, the layer that is\n    /// allowed to touch the filesystem, so the Fs effect is performed here.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, entry: &str) {\n            use std::io::Write;\n            // COMPLIANCE: audit every order to the local log (a filesystem write).\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", entry);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = FileAuditSink;\n        domain::place_order(id, &mut audit)\n    }\n}"}, {"fixture": "audit", "model": "opus", "arm": "treatment", "i": 9, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure: it performs no filesystem effect. The\n    /// COMPLIANCE audit write has been hoisted to the `api` boundary\n    /// layer (see `api::handle`), which is allowed to touch the\n    /// filesystem under the `deny Fs domain` policy.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted out of `domain` to this allowed boundary layer so the\n        // domain stays pure while every order is still audited.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 0, "editedLibRs": "pub mod domain {\n    /// An audit record that a caller (outside `domain`) is responsible for\n    /// persisting. `domain` never touches the filesystem itself \u2014 it only\n    /// describes what must be audited.\n    pub struct AuditRecord {\n        pub line: String,\n    }\n\n    /// Place an order and return its confirmation number, along with the\n    /// COMPLIANCE audit record that the caller MUST persist.\n    ///\n    /// This function performs no I/O \u2014 it is pure domain logic. Writing the\n    /// audit record to the filesystem is the caller's (`api`'s) job.\n    pub fn place_order(id: u64) -> (u64, AuditRecord) {\n        let confirmation = id + 1;\n        let audit = AuditRecord {\n            line: format!(\"order {}\", id),\n        };\n        (confirmation, audit)\n    }\n}\n\npub mod api {\n    use crate::domain::AuditRecord;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit) = crate::domain::place_order(id);\n        write_audit(&audit);\n        confirmation\n    }\n\n    /// COMPLIANCE: persist the audit record to the local log (a filesystem\n    /// write). Lives in `api`, which is permitted to perform Fs.\n    fn write_audit(record: &AuditRecord) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", record.line);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 0, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: performs no filesystem effects. Compliance auditing is the\n    /// caller's responsibility (see `api::handle`), which hoists the\n    /// audit-log write to the boundary layer that is allowed to perform Fs.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted here from `domain::place_order` so the domain module stays\n        // free of the Fs effect per policy `deny Fs domain`.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 1, "editedLibRs": "pub mod domain {\n    /// A sink for compliance audit records. `domain` only depends on this\n    /// abstract port \u2014 it performs no filesystem (or other) I/O itself.\n    /// Concrete, effectful implementations live in outer layers (e.g. `api`).\n    pub trait AuditSink {\n        fn record(&self, message: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST be audited. The audit write itself is\n    /// performed by whatever `AuditSink` the caller injects, keeping this\n    /// function free of the Fs effect while still guaranteeing the audit\n    /// happens on every call \u2014 it is not optional and cannot be skipped.\n    pub fn place_order(id: u64, audit: &dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::{self, AuditSink};\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&self, message: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", message);\n            }\n        }\n    }\n\n    pub fn handle(id: u64) -> u64 {\n        let sink = FileAuditSink;\n        domain::place_order(id, &sink)\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 1, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: performs no I/O. The audit-log write is a boundary\n    /// concern and is hoisted out to `api::handle`, which is an\n    /// allowed layer for the Fs effect.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here at the boundary, not inside `domain`.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 2, "editedLibRs": "pub mod domain {\n    /// Port the domain layer depends on to record compliance audit\n    /// entries, without knowing (or caring) how they're persisted.\n    /// No `std::fs` (or any I/O) appears anywhere in this module \u2014 the\n    /// concrete, filesystem-touching implementation lives in `api`.\n    pub trait AuditSink {\n        fn record(&mut self, entry: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST still be audited. `domain` performs\n    /// no Fs itself here \u2014 it delegates to whatever `AuditSink` the\n    /// caller supplies (see `api::FileAuditSink`), so the audit\n    /// guarantee is preserved while `domain` stays Fs-free.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, entry: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", entry);\n            }\n        }\n    }\n\n    pub fn handle(id: u64) -> u64 {\n        let mut sink = FileAuditSink;\n        crate::domain::place_order(id, &mut sink)\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 2, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: performs no filesystem effects. The caller (an allowed layer)\n    /// is responsible for auditing the order via the returned `AuditEntry`.\n    pub fn place_order(id: u64) -> (u64, AuditEntry) {\n        let confirmation = id + 1;\n        let entry = AuditEntry {\n            message: format!(\"order {}\", id),\n        };\n        (confirmation, entry)\n    }\n\n    /// A pending audit record describing what must be logged.\n    /// Producing this value performs no I/O \u2014 writing it out is left to\n    /// an allowed (non-domain) layer.\n    pub struct AuditEntry {\n        pub message: String,\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, entry) = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here, at the boundary, not inside `domain`.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", entry.message);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 3, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 3, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function stays pure (no Fs): instead of writing to the audit log\n    /// itself, it returns the audit line as a plain value (a \"port\"). The\n    /// caller \u2014 an allowed layer \u2014 is responsible for actually performing the\n    /// write. The compliance requirement (every order is audited) is met by\n    /// `api::handle` always writing the returned line before returning.\n    pub fn place_order(id: u64) -> (u64, String) {\n        let confirmation = id + 1;\n        let audit_line = format!(\"order {}\", id);\n        (confirmation, audit_line)\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit_line) = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here, at the boundary, so `domain` stays free of the Fs effect.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"{}\", audit_line);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 4, "editedLibRs": "pub mod domain {\n    /// The audit record a domain operation requires. Pure data \u2014 no I/O here.\n    /// Producing this does not perform Fs; it is the caller's job (in a layer\n    /// that is allowed to touch the filesystem) to actually persist it.\n    pub struct AuditRecord {\n        pub line: String,\n    }\n\n    /// Place an order and return its confirmation number plus the audit\n    /// record that COMPLIANCE requires be written to the log. This function\n    /// itself performs no filesystem access \u2014 it only describes what must\n    /// be audited, leaving the actual write to a layer permitted to do Fs.\n    pub fn place_order(id: u64) -> (u64, AuditRecord) {\n        let confirmation = id + 1;\n        let record = AuditRecord {\n            line: format!(\"order {}\", id),\n        };\n        (confirmation, record)\n    }\n}\npub mod api {\n    use crate::domain::AuditRecord;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, record) = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // This always runs \u2014 there is no path through `handle` that skips it.\n        write_audit(&record);\n        confirmation\n    }\n\n    fn write_audit(record: &AuditRecord) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", record.line);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 4, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: performs no Fs. The audit write is hoisted to the `api`\n    /// boundary layer, which is allowed to touch the filesystem.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here at the boundary, not in `domain`, so the domain\n        // stays pure while every order is still unconditionally audited.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 5, "editedLibRs": "pub mod domain {\n    /// A port the domain depends on to record compliance audit events.\n    /// `domain` only sees this trait \u2014 never the filesystem itself.\n    pub trait AuditSink {\n        fn record(&mut self, message: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST be audited. The write itself happens\n    /// behind the `AuditSink` port supplied by the caller, so `domain`\n    /// stays free of the Fs effect while the audit still always happens.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, message: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", message);\n            }\n        }\n    }\n\n    pub fn handle(id: u64) -> u64 {\n        let mut sink = FileAuditSink;\n        crate::domain::place_order(id, &mut sink)\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 5, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: performs no I/O. Callers at the boundary (see `api::handle`)\n    /// are responsible for auditing the order using the returned record.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here, at the boundary, so `domain` stays pure.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 6, "editedLibRs": "pub mod domain {\n    /// A port (trait) that `domain` depends on abstractly. `domain` never\n    /// touches the filesystem itself \u2014 it only calls this trait, so the\n    /// actual Fs effect lives outside the `domain` module entirely.\n    pub trait AuditSink {\n        fn record(&self, line: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order must still be audited. The audit write is\n    /// performed by whatever `AuditSink` the caller supplies \u2014 `domain`\n    /// itself performs no Fs effect.\n    pub fn place_order(id: u64, audit: &dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// The concrete audit sink \u2014 this layer is allowed to touch the\n    /// filesystem, so the actual audit-log write happens here.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&self, line: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let sink = FileAuditSink;\n        crate::domain::place_order(id, &sink)\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 6, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: performs no filesystem effects. Callers at the boundary\n    /// (e.g. `api::handle`) are responsible for auditing the order.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Performed here, at the boundary, so `domain` stays free of the Fs effect.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 7, "editedLibRs": "pub mod domain {\n    /// A port that `domain` depends on to record compliance audit entries.\n    /// `domain` never touches the filesystem itself \u2014 it only calls this trait,\n    /// whose implementation is supplied by an outer (non-domain) layer.\n    pub trait AuditLog {\n        fn record(&mut self, message: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST be audited. The audit write is performed\n    /// by the caller-supplied `AuditLog` implementation, keeping `domain`\n    /// itself free of the Fs effect.\n    pub fn place_order(id: u64, audit: &mut impl AuditLog) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditLog;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    struct FileAuditLog;\n\n    impl AuditLog for FileAuditLog {\n        fn record(&mut self, message: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", message);\n            }\n        }\n    }\n\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = FileAuditLog;\n        crate::domain::place_order(id, &mut audit)\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 7, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure domain logic: no filesystem access here (policy: `deny Fs domain`).\n    /// Compliance still requires every order to be audited to the log \u2014 that\n    /// write is performed by the caller (see `api::handle`), which is allowed\n    /// to touch the filesystem. This function returns the audit line the\n    /// caller must persist.\n    pub fn place_order(id: u64) -> (u64, String) {\n        let confirmation = id + 1;\n        let audit_line = format!(\"order {}\", id);\n        (confirmation, audit_line)\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit_line) = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"{}\", audit_line);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 8, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure domain logic: no filesystem access here (policy: `deny Fs domain`).\n    /// Compliance still requires every order to be audited to the log \u2014 that\n    /// write is performed by the caller (see `api::handle`), which is allowed\n    /// to touch the filesystem. This function returns the audit line the\n    /// caller must persist.\n    pub fn place_order(id: u64) -> (u64, String) {\n        let confirmation = id + 1;\n        let audit_line = format!(\"order {}\", id);\n        (confirmation, audit_line)\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit_line) = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"{}\", audit_line);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 8, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: performs no filesystem (or other) effects. Auditing is\n    /// the caller's responsibility \u2014 see `api::handle`, which is the\n    /// allowed boundary layer for the Fs effect.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted here from `domain::place_order` so the domain module stays\n        // free of the Fs effect, per the `deny Fs domain` architecture policy.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "control", "i": 9, "editedLibRs": "pub mod domain {\n    /// An order confirmation, paired with the audit line that MUST be\n    /// written to the audit log by a layer that is allowed to touch Fs.\n    pub struct OrderResult {\n        pub confirmation: u64,\n        pub audit_line: String,\n    }\n\n    /// Place an order and return its confirmation number plus the audit\n    /// record to be persisted. This function is pure \u2014 no filesystem\n    /// access \u2014 so it stays compliant with `deny Fs domain`. The caller\n    /// (an Fs-allowed layer) is responsible for actually writing the\n    /// audit line; the compliance requirement is enforced by contract:\n    /// every `OrderResult` carries the line that MUST be audited.\n    pub fn place_order(id: u64) -> OrderResult {\n        let confirmation = id + 1;\n        OrderResult {\n            confirmation,\n            audit_line: format!(\"order {}\", id),\n        }\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    /// It performs the domain logic, then discharges the COMPLIANCE\n    /// requirement by writing the returned audit line to the local log.\n    pub fn handle(id: u64) -> u64 {\n        use std::io::Write;\n\n        let result = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"{}\", result.audit_line);\n        }\n\n        result.confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "sonnet", "arm": "treatment", "i": 9, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted out of `domain` to the boundary layer, per the architecture gate fix.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 0, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 0, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 1, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 1, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}\n"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 2, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 2, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}\n"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 3, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 3, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 4, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 4, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 5, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 5, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 6, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}\n"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 6, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 7, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 7, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 8, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 8, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}\n"}, {"fixture": "audit", "model": "haiku", "arm": "control", "i": 9, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "haiku", "arm": "treatment", "i": 9, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// This function is pure with respect to the filesystem: it performs no\n    /// Fs effect itself. Compliance still requires every order to be\n    /// audited, so the caller must supply an `audit` callback that this\n    /// function invokes with the audit line \u2014 the actual filesystem write\n    /// happens in the caller (see `api::handle`), which is permitted to\n    /// touch Fs.\n    pub fn place_order(id: u64, mut audit: impl FnMut(&str)) -> u64 {\n        // COMPLIANCE: audit every order. The write itself is delegated to\n        // the caller via `audit`, keeping this module free of the Fs effect.\n        audit(&format!(\"order {}\", id));\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        crate::domain::place_order(id, |line| {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        })\n    }\n}"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 0, "editedLibRs": "pub mod domain {\n    /// Port for the compliance audit trail. The domain layer *requires* that\n    /// every order is audited, but it does not perform the write itself \u2014\n    /// the effectful implementation is injected by an outer layer (see `api`).\n    pub trait AuditSink {\n        fn record(&mut self, entry: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST be audited. The domain enforces this by\n    /// unconditionally emitting the audit entry through the supplied sink;\n    /// the filesystem effect lives in the caller's sink implementation, so\n    /// `domain` itself performs no Fs.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// Filesystem-backed audit sink. This layer is allowed to touch the\n    /// filesystem, so the audit-log write (the Fs effect) lives here.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, entry: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", entry);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = FileAuditSink;\n        crate::domain::place_order(id, &mut audit)\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 0, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: the compliance audit (an Fs effect) is performed by the caller\n    /// at the boundary layer \u2014 see `api::handle`.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // hoisted out of the domain to this allowed boundary layer.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        crate::domain::place_order(id)\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 1, "editedLibRs": "pub mod domain {\n    /// A placed order: the confirmation number plus the audit record that\n    /// MUST be persisted by an outer layer (COMPLIANCE: every order is audited).\n    pub struct PlacedOrder {\n        /// The confirmation number for the order.\n        pub confirmation: u64,\n        /// The audit-log line for this order. `domain` is forbidden from\n        /// touching the filesystem (`deny Fs domain`), so it emits the audit\n        /// record as data; the effectful layer (`api`) writes it to the log.\n        pub audit_line: String,\n    }\n\n    /// Place an order. Free of filesystem effects: returns the confirmation\n    /// number together with the audit record the caller must persist.\n    pub fn place_order(id: u64) -> PlacedOrder {\n        PlacedOrder {\n            confirmation: id + 1,\n            audit_line: format!(\"order {}\", id),\n        }\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let order = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // The audit record comes from `domain`; the Fs effect happens here,\n        // in the layer permitted to perform it.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", order.audit_line);\n        }\n        order.confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 1, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure business logic: no filesystem access here (policy: `deny Fs domain`).\n    /// The COMPLIANCE audit write is performed by the caller at the `api` boundary.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // hoisted out of the domain boundary per `deny Fs domain`.\n        audit_order(id);\n        crate::domain::place_order(id)\n    }\n\n    /// Append the order to the audit log. Fs is allowed at this layer.\n    fn audit_order(id: u64) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 2, "editedLibRs": "pub mod domain {\n    /// An audit record produced by the domain. Persisting it is a COMPLIANCE\n    /// requirement \u2014 the outer layer that receives it MUST write it to the\n    /// audit log. `#[must_use]` makes it a compiler warning to drop it.\n    #[must_use = \"audit events must be persisted to the audit log (compliance)\"]\n    pub struct AuditEvent {\n        pub line: String,\n    }\n\n    /// Place an order and return its confirmation number plus the audit\n    /// event that the caller is required to persist. The domain describes\n    /// the audit as data; it performs no filesystem effect itself\n    /// (policy: `deny Fs domain`).\n    pub fn place_order(id: u64) -> (u64, AuditEvent) {\n        let event = AuditEvent {\n            line: format!(\"order {}\", id),\n        };\n        (id + 1, event)\n    }\n}\n\npub mod audit {\n    use crate::domain::AuditEvent;\n\n    /// Persist an audit event to the local log. This is the filesystem\n    /// write \u2014 it lives OUTSIDE `domain`, in a layer where Fs is allowed.\n    pub fn persist(event: &AuditEvent) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", event.line);\n        }\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    /// COMPLIANCE: every order placed through this entry point is audited.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, event) = crate::domain::place_order(id);\n        crate::audit::persist(&event);\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 2, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: no filesystem access here. The COMPLIANCE audit write is\n    /// performed by the boundary layer (`api::handle`), which audits\n    /// every order before delegating into the domain.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem\n        // write), hoisted out of the `domain` boundary per `deny Fs domain`.\n        audit_order(id);\n        crate::domain::place_order(id)\n    }\n\n    /// Append the order to the audit log. Fs is permitted in this layer.\n    fn audit_order(id: u64) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 3, "editedLibRs": "pub mod domain {\n    /// Where audit records go. Implementations live OUTSIDE `domain`,\n    /// so the domain layer stays free of filesystem effects.\n    pub trait AuditSink {\n        fn record(&mut self, line: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order is audited \u2014 the caller MUST supply a sink,\n    /// and the audit record is written before returning. The Fs effect lives\n    /// in the sink implementation (outer layer), not in `domain`.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod audit {\n    use std::io::Write;\n\n    /// File-backed audit sink. This layer is allowed to touch the filesystem.\n    pub struct FileAuditLog;\n\n    impl crate::domain::AuditSink for FileAuditLog {\n        fn record(&mut self, line: &str) {\n            // COMPLIANCE: audit every order to the local log (a filesystem write).\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", line);\n            }\n        }\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = crate::audit::FileAuditLog;\n        crate::domain::place_order(id, &mut audit)\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 3, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure by policy (`deny Fs domain`): the COMPLIANCE audit write is\n    /// hoisted to the boundary layer \u2014 see `api::handle`, which audits\n    /// every order before delegating here.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // Hoisted out of `domain` per policy `deny Fs domain`.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        crate::domain::place_order(id)\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 4, "editedLibRs": "pub mod domain {\n    /// An audit record produced by the domain. Persisting it is a COMPLIANCE\n    /// requirement \u2014 the outer layer that receives it MUST write it to the\n    /// audit log. `#[must_use]` makes it a compiler warning to drop it.\n    #[must_use = \"audit events must be persisted to the audit log (compliance)\"]\n    pub struct AuditEvent {\n        pub line: String,\n    }\n\n    /// Place an order and return its confirmation number plus the audit\n    /// event that the caller is required to persist. The domain describes\n    /// the audit as data; it performs no filesystem effect itself\n    /// (policy: `deny Fs domain`).\n    pub fn place_order(id: u64) -> (u64, AuditEvent) {\n        let event = AuditEvent {\n            line: format!(\"order {}\", id),\n        };\n        (id + 1, event)\n    }\n}\n\npub mod audit {\n    use crate::domain::AuditEvent;\n\n    /// Persist an audit event to the local log. This is the filesystem\n    /// write \u2014 it lives OUTSIDE `domain`, in a layer where Fs is allowed.\n    pub fn persist(event: &AuditEvent) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", event.line);\n        }\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    /// COMPLIANCE: every order placed through this entry point is audited.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, event) = crate::domain::place_order(id);\n        crate::audit::persist(&event);\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 4, "editedLibRs": "pub mod domain {\n    /// Port for the compliance audit trail. The domain layer *requires* that\n    /// every order is audited, but it does not perform the write itself \u2014\n    /// the effectful implementation is injected by an outer layer (see `api`).\n    pub trait AuditSink {\n        fn record(&mut self, entry: &str);\n    }\n\n    /// Place an order and return its confirmation number.\n    ///\n    /// COMPLIANCE: every order MUST be audited. The domain enforces this by\n    /// unconditionally emitting the audit entry through the supplied sink;\n    /// the filesystem effect lives in the caller's sink implementation, so\n    /// `domain` itself performs no Fs.\n    pub fn place_order(id: u64, audit: &mut dyn AuditSink) -> u64 {\n        audit.record(&format!(\"order {}\", id));\n        id + 1\n    }\n}\n\npub mod api {\n    use crate::domain::AuditSink;\n\n    /// Filesystem-backed audit sink. This layer is allowed to touch the\n    /// filesystem, so the audit-log write (the Fs effect) lives here.\n    struct FileAuditSink;\n\n    impl AuditSink for FileAuditSink {\n        fn record(&mut self, entry: &str) {\n            use std::io::Write;\n            if let Ok(mut f) = std::fs::OpenOptions::new()\n                .create(true)\n                .append(true)\n                .open(\"audit.log\")\n            {\n                let _ = writeln!(f, \"{}\", entry);\n            }\n        }\n    }\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let mut audit = FileAuditSink;\n        crate::domain::place_order(id, &mut audit)\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 5, "editedLibRs": "pub mod domain {\n    /// The outcome of placing an order: the confirmation number plus the\n    /// audit record that the caller MUST persist.\n    ///\n    /// COMPLIANCE: every order must be audited. The domain produces the\n    /// audit record as data; persisting it (the Fs effect) is the adapter\n    /// layer's responsibility, keeping `domain` free of filesystem effects\n    /// per policy `deny Fs domain`.\n    #[must_use = \"the audit record must be persisted \u2014 every order must be audited\"]\n    pub struct PlacedOrder {\n        pub confirmation: u64,\n        pub audit_record: String,\n    }\n\n    /// Place an order: compute the confirmation number and the audit record.\n    /// No filesystem access happens here.\n    pub fn place_order(id: u64) -> PlacedOrder {\n        PlacedOrder {\n            confirmation: id + 1,\n            audit_record: format!(\"order {}\", id),\n        }\n    }\n}\n\npub mod api {\n    use std::io::Write;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let placed = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // performed here in the adapter layer where Fs is permitted.\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", placed.audit_record);\n        }\n        placed.confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 5, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: no filesystem effects here. The COMPLIANCE audit write is\n    /// performed by the calling boundary layer (`api::handle`), which is\n    /// allowed to touch the filesystem.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let confirmation = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // hoisted out of the domain so it stays pure.\n        audit(id);\n        confirmation\n    }\n\n    /// Append the order to the local audit log.\n    fn audit(id: u64) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 6, "editedLibRs": "pub mod domain {\n    /// An order confirmation, paired with the audit line that MUST be\n    /// written to the audit log by a layer that is allowed to touch Fs.\n    pub struct OrderResult {\n        pub confirmation: u64,\n        pub audit_line: String,\n    }\n\n    /// Place an order and return its confirmation number plus the audit\n    /// record to be persisted. This function is pure \u2014 no filesystem\n    /// access \u2014 so it stays compliant with `deny Fs domain`. The caller\n    /// (an Fs-allowed layer) is responsible for actually writing the\n    /// audit line; the compliance requirement is enforced by contract:\n    /// every `OrderResult` carries the line that MUST be audited.\n    pub fn place_order(id: u64) -> OrderResult {\n        let confirmation = id + 1;\n        OrderResult {\n            confirmation,\n            audit_line: format!(\"order {}\", id),\n        }\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    /// It performs the domain logic, then discharges the COMPLIANCE\n    /// requirement by writing the returned audit line to the local log.\n    pub fn handle(id: u64) -> u64 {\n        use std::io::Write;\n\n        let result = crate::domain::place_order(id);\n\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"{}\", result.audit_line);\n        }\n\n        result.confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 6, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure by policy (`deny Fs domain`): the compliance audit write is\n    /// performed by the calling boundary layer (see `api::handle`).\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // hoisted out of the domain so `domain` stays free of Fs.\n        audit_order(id);\n        crate::domain::place_order(id)\n    }\n\n    /// Append the order to the local audit log.\n    fn audit_order(id: u64) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 7, "editedLibRs": "pub mod domain {\n    /// A pure description of the audit-log entry that MUST be recorded for\n    /// every order. Constructing it performs no effects; persisting it is the\n    /// caller's job. `#[must_use]` makes silently dropping the audit a\n    /// compiler warning, so the compliance obligation travels with the value.\n    #[must_use = \"COMPLIANCE: every order must be audited \u2014 write this entry to the log\"]\n    pub struct AuditEntry {\n        pub line: String,\n    }\n\n    /// Place an order and return its confirmation number together with the\n    /// audit entry that the calling layer is obliged to persist.\n    ///\n    /// Policy: `deny Fs domain` \u2014 this module never touches the filesystem.\n    pub fn place_order(id: u64) -> (u64, AuditEntry) {\n        // COMPLIANCE: the audit record is produced here (pure data); the api\n        // layer, where the Fs effect is permitted, performs the actual write.\n        let entry = AuditEntry {\n            line: format!(\"order {}\", id),\n        };\n        (id + 1, entry)\n    }\n}\n\npub mod api {\n    use std::io::Write;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, entry) = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log. The filesystem\n        // write happens here in the api layer, where Fs is allowed.\n        write_audit(&entry);\n        confirmation\n    }\n\n    /// Append an audit entry to the local audit log (the Fs effect lives here).\n    fn write_audit(entry: &crate::domain::AuditEntry) {\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", entry.line);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 7, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure by policy (`deny Fs domain`): the compliance audit write is\n    /// performed by the calling boundary layer (see `api::handle`).\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // hoisted out of the domain so `domain` stays free of Fs.\n        audit_order(id);\n        crate::domain::place_order(id)\n    }\n\n    /// Append the order to the local audit log.\n    fn audit_order(id: u64) {\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 8, "editedLibRs": "pub mod domain {\n    /// An audit record describing an order. The domain layer produces this\n    /// value; an outer layer (api) is responsible for persisting it. This\n    /// keeps the domain free of Fs effects while preserving the compliance\n    /// requirement that every order is audited.\n    pub struct AuditRecord {\n        pub line: String,\n    }\n\n    /// Place an order and return its confirmation number along with the\n    /// audit record that MUST be written to the log by the caller.\n    /// Pure: performs no filesystem access (policy: `deny Fs domain`).\n    pub fn place_order(id: u64) -> (u64, AuditRecord) {\n        let audit = AuditRecord {\n            line: format!(\"order {}\", id),\n        };\n        (id + 1, audit)\n    }\n}\n\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let (confirmation, audit) = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // The Fs effect lives here in the api layer, where policy permits it.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", audit.line);\n        }\n        confirmation\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 8, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    ///\n    /// Pure: no filesystem effects here (policy: `deny Fs domain`). The\n    /// COMPLIANCE audit write is hoisted to the boundary (`api::handle`),\n    /// the layer that is allowed to perform Fs.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\n\npub mod api {\n    use std::io::Write;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem\n        // write), performed here at the boundary so the domain stays pure.\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        crate::domain::place_order(id)\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "control", "i": 9, "editedLibRs": "pub mod domain {\n    /// Proof-of-audit obligation: placing an order yields this record, and the\n    /// caller MUST persist it (COMPLIANCE: every order is audited). The domain\n    /// layer only *describes* the audit entry \u2014 it never touches the filesystem,\n    /// per policy `deny Fs domain`.\n    #[must_use = \"every order MUST be audited: write this record to the audit log\"]\n    pub struct AuditRecord {\n        pub line: String,\n    }\n\n    /// Outcome of placing an order: the confirmation number plus the audit\n    /// record the caller is obliged to persist.\n    #[must_use = \"contains an AuditRecord that MUST be written to the audit log\"]\n    pub struct PlacedOrder {\n        pub confirmation: u64,\n        pub audit: AuditRecord,\n    }\n\n    /// Place an order and return its confirmation number together with the\n    /// audit record. Pure domain logic \u2014 no filesystem effect here.\n    pub fn place_order(id: u64) -> PlacedOrder {\n        PlacedOrder {\n            confirmation: id + 1,\n            audit: AuditRecord {\n                line: format!(\"order {}\", id),\n            },\n        }\n    }\n}\n\npub mod api {\n    use crate::domain::AuditRecord;\n    use std::io::Write;\n\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        let placed = crate::domain::place_order(id);\n        // COMPLIANCE: audit every order to the local log (a filesystem write).\n        // The Fs effect lives here in the api layer, where policy permits it.\n        write_audit(placed.audit);\n        placed.confirmation\n    }\n\n    /// Persist an audit record to the local audit log.\n    fn write_audit(record: AuditRecord) {\n        if let Ok(mut f) = std::fs::OpenOptions::new()\n            .create(true)\n            .append(true)\n            .open(\"audit.log\")\n        {\n            let _ = writeln!(f, \"{}\", record.line);\n        }\n    }\n}\n"}, {"fixture": "audit", "model": "fable", "arm": "treatment", "i": 9, "editedLibRs": "pub mod domain {\n    /// Place an order and return its confirmation number.\n    /// Pure: no filesystem access \u2014 the audit write is hoisted to the\n    /// `api` boundary (see `api::handle`), per policy `deny Fs domain`.\n    pub fn place_order(id: u64) -> u64 {\n        id + 1\n    }\n}\npub mod api {\n    /// The request handler \u2014 this layer is allowed to touch the filesystem.\n    pub fn handle(id: u64) -> u64 {\n        // COMPLIANCE: audit every order to the local log (a filesystem write),\n        // performed here at the boundary so the domain stays pure.\n        use std::io::Write;\n        if let Ok(mut f) = std::fs::OpenOptions::new().create(true).append(true).open(\"audit.log\") {\n            let _ = writeln!(f, \"order {}\", id);\n        }\n        crate::domain::place_order(id)\n    }\n}\n"}]
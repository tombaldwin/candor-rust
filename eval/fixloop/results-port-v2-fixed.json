[{"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 0, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic: takes the fetched rate as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Top-level entry point ABOVE the `domain` layer. It performs Net here\n/// (in an allowed layer), then passes the result DOWN as a plain value.\n/// Invoked by the scheduler.\npub fn run_batch() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    crate::domain::run(rate)\n}\n"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 1, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch step: given a fetched rate, produce the price. Takes Net-derived\n    /// data as a plain value, so the `domain` layer stays free of Net.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n/// Lives ABOVE the `domain` layer: it performs Net here and passes the rate DOWN as a\n/// plain value, keeping every `domain` function pure.\npub fn run() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    crate::domain::run(rate)\n}\n"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 2, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch computation; the rate is supplied from above as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Top-level entry point ABOVE the `domain` layer: it performs Net (fetches the\n/// rate) and passes the result DOWN into the pure `domain` layer as a value.\npub fn run() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    crate::domain::run(rate)\n}\n"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 3, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic: takes the rate as a plain value and stays free of Net.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Top-level entry point ABOVE the `domain` layer: it performs Net (fetches the\n/// rate) and passes the result DOWN into the pure `domain` functions as a value.\n/// The scheduler invokes this; `domain` never touches the network.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 4, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch computation over an already-fetched rate.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler, not from inside\n/// this crate). Lives ABOVE the `domain` layer: it performs Net here and passes\n/// the fetched rate DOWN into the pure `domain` functions as a plain value.\npub fn run() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    crate::domain::run(rate)\n}\n"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 5, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// The domain-layer batch step. Pure: the rate is supplied from above as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler).\n/// This thin function lives ABOVE the `domain` layer: it performs Net here,\n/// then passes the fetched rate DOWN into `domain` as a plain value so the\n/// `domain` functions stay pure and the `deny Net domain` boundary holds.\npub fn run_batch() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    crate::domain::run(rate)\n}"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 6, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch core: takes the already-fetched rate as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n/// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n/// Lives ABOVE the `domain` layer: it performs Net, then passes the rate DOWN as a plain value.\npub fn run() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    crate::domain::run(rate)\n}\n"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 7, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch computation over an already-fetched rate.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Top-level batch entry point (invoked by the scheduler).\n/// Lives ABOVE the `domain` layer: it performs Net here and passes the\n/// fetched rate DOWN as a plain value, keeping `domain` pure.\npub fn run() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    crate::domain::run(rate)\n}"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 8, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch computation: takes the already-fetched rate as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Top-level entry point ABOVE the `domain` layer: performs Net here, then\n/// passes the fetched rate DOWN into the pure `domain` functions as a value.\npub fn run() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "opus", "arm": "treatment-v2", "i": 9, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler, not from inside\n/// this crate). It lives ABOVE the `domain` layer, performs Net here to fetch\n/// the rate, and passes the result DOWN into `domain` as a plain value so the\n/// `domain` functions stay pure.\npub fn run() -> u64 {\n    let rate = crate::infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 0, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 1, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Thin entry point ABOVE the `domain` layer: performs Net, then passes the\n/// fetched rate down as a plain value so `domain` stays pure.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 2, "editedLibRs": "pub mod infra {\n    /// Boundary adapter: the only place in the crate that touches the network.\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\npub mod domain {\n    //! Pure pricing logic. Effects stay at the boundary: callers fetch the\n    //! rate (see `crate::app::run`) and pass it in as plain data, so nothing\n    //! in this module performs Net.\n\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from\n    /// inside this crate). Fetches the rate at the boundary, then hands pure\n    /// data to the domain.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 3, "editedLibRs": "pub mod infra {\n    /// Boundary adapter: the only place in the crate that touches the network.\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\npub mod domain {\n    //! Pure pricing logic. Effects stay at the boundary: callers fetch the\n    //! rate (see `crate::app::run`) and pass it in as plain data, so nothing\n    //! in this module performs Net.\n\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from\n    /// inside this crate). Fetches the rate at the boundary, then hands pure\n    /// data to the domain.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 4, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 5, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 6, "editedLibRs": "pub mod infra {\n    /// Boundary adapter: the only place in the crate that touches the network.\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\npub mod domain {\n    //! Pure pricing logic. Effects stay at the boundary: the caller fetches\n    //! the rate (see `crate::app::run`) and passes it in as plain data, so\n    //! nothing in this module performs Net.\n\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from\n    /// inside this crate). Fetches the rate at the boundary, then hands pure\n    /// data down to the `domain` layer.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 7, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch step: takes the already-fetched rate as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler).\n/// Lives above the `domain` boundary: performs Net here and passes the\n/// result down as a plain value, keeping `domain` free of the Net effect.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 8, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "sonnet", "arm": "treatment-v2", "i": 9, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 0, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 1, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 2, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 3, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 4, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 5, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}\n"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 6, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 7, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 8, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "haiku", "arm": "treatment-v2", "i": 9, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    /// Business logic \u2014 pure function of its input. The domain no longer reaches\n    /// out to `infra` itself; the rate is a PORT (plain parameter) supplied by a\n    /// caller in an allowed layer, so no Net effect is reachable from `domain`.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic, now taking its rate as input instead of fetching it.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// The top-level batch entry point (invoked by the scheduler). This adapter layer\n/// is allowed to perform Net: it fetches the rate via `infra` and injects it into\n/// the `domain` (dependency inversion) \u2014 the fetch still genuinely happens.\npub fn run() -> u64 {\n    let rate = infra::fetch_rate();\n    domain::run(rate)\n}"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 0, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\n/// Pure pricing logic: the rate arrives as a plain value, so nothing in\n/// this module performs (or transitively reaches) Net.\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Thin entry layer ABOVE `domain`: it performs the Net fetch and passes\n/// the result DOWN as a value.\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from\n    /// inside this crate). Still actually fetches the rate, then produces\n    /// the price via the pure `domain` layer.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 1, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\npub mod domain {\n    /// Pure: computes the price from a rate passed down as a plain value.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch pipeline; the rate is fetched above this boundary.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Entry layer ABOVE the `domain` boundary: performs the Net fetch here and\n/// hands the result down to `domain` as a plain value, keeping `domain` pure.\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler).\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 2, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\n/// Pure pricing logic: the rate arrives as a plain value, so nothing in\n/// this module performs (or transitively reaches) Net.\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Thin entry layer ABOVE `domain`: it performs the Net fetch and passes\n/// the result DOWN as a value.\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from\n    /// inside this crate). Still actually fetches the rate, then produces\n    /// the price via the pure `domain` layer.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 3, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\npub mod domain {\n    /// Pure: computes the price from a rate passed down as a plain value.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch pipeline; the rate is fetched above this boundary.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n\n/// Entry layer ABOVE the `domain` boundary: performs the Net fetch here and\n/// hands the result down to `domain` as a plain value, keeping `domain` pure.\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler).\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 4, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    //! Pure pricing logic: the exchange rate arrives as a plain value, so no\n    //! function in this module performs (or transitively reaches) Net.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch computation over an already-fetched rate.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n    /// Lives ABOVE the `domain` boundary: it performs the Net fetch and hands the\n    /// rate DOWN to `domain` as a plain value.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 5, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch logic: computes the price from an already-fetched rate.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n    /// Performs Net here, above the `domain` boundary, and passes the rate down as a value.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 6, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\n\n/// Entry layer ABOVE `domain`: performs the Net effect once, then hands the\n/// result down to the pure `domain` layer as a plain value.\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler).\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure domain entry: takes the already-fetched rate as a value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 7, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    //! Pure pricing logic: the exchange rate arrives as a plain value, so no\n    //! function in this module performs (or transitively reaches) Net.\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch computation over an already-fetched rate.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n    /// Lives ABOVE the `domain` boundary: it performs the Net fetch and hands the\n    /// rate DOWN to `domain` as a plain value.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 8, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch pipeline: the current rate is passed down as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n    /// Sits ABOVE the `domain` layer: it performs the Net effect (fetching the rate) and\n    /// hands the result down to the pure `domain` pipeline as a plain value.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}, {"fixture": "port", "model": "fable", "arm": "treatment-v2", "i": 9, "editedLibRs": "pub mod infra {\n    pub fn fetch_rate() -> u64 {\n        let _ = std::net::TcpStream::connect(\"rates.example.com:443\");\n        100\n    }\n}\npub mod domain {\n    pub fn price(rate: u64) -> u64 {\n        rate + 1\n    }\n    pub fn process(rate: u64) -> u64 {\n        price(rate) * 2\n    }\n    /// Pure batch pipeline: the current rate is passed down as a plain value.\n    pub fn run(rate: u64) -> u64 {\n        process(rate)\n    }\n}\npub mod app {\n    /// The top-level batch entry point (invoked by the scheduler, not from inside this crate).\n    /// Sits ABOVE the `domain` layer: it performs the Net effect (fetching the rate) and\n    /// hands the result down to the pure `domain` pipeline as a plain value.\n    pub fn run() -> u64 {\n        let rate = crate::infra::fetch_rate();\n        crate::domain::run(rate)\n    }\n}\n"}]
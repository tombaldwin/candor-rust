[INCOMPLETE] The trial agent's auto-mode safety classifier blocked the `std::process::Command` edit
(the exec task) and asked for permission instead of completing the change. No "## Summary" was
produced and the task was not implemented. Per the pre-registered protocol this trial is excluded from
the primary (completeness) and binary metrics, and recorded here rather than silently dropped.

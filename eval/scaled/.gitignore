# Generated by harness.sh — the cached per-task baselines and each trial's working copy / build
# artifacts are regenerable; the committed run record is PROMPT.md / summary.md / verdict.txt / meta.tsv.
/.cache
runs*/*/work/
runs*/*/target/
runs*/*/baseline.*.json

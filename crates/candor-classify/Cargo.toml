[package]
name = "candor-classify"
version = "0.3.0"
edition = "2021"
description = "candor's curated effect classifier (crate+path -> effect) — pure, stable Rust."
license = "MIT OR Apache-2.0"

[dependencies]
candor-report = { path = "../candor-report", version = "0.3.0" }

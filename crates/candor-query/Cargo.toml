[package]
name = "candor-query"
version = "0.4.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "candor's read-only report queries (show/where/callers/map/diff) in Rust — used by cargo-candor."
homepage = "https://candor.poly.io"
repository = "https://github.com/tombaldwin/candor-rust"

[[bin]]
name = "candor-query"
path = "src/main.rs"

[dependencies]
candor-report = { path = "../candor-report" }
candor-classify = { path = "../candor-classify" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
